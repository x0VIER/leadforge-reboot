version = 1
id = "leadforge-florida-loop"
kind = "heartbeat"
name = "LeadForge Lead Factory Loop"
status = "ACTIVE"
rrule = "FREQ=MINUTELY;INTERVAL=30"

# This file is the repo-owned reference for the Codex app heartbeat.
# The live automation lives under CODEX_HOME/automations and should match this intent.
prompt = """
Continue the LeadForge lead factory in C:\Users\loc9o\Desktop\AGR 1226. Treat this as an ongoing Codex automation loop, not a one-shot summary.
First read LeadForge_Reboot\agent_shared\status\OPS_SNAPSHOT.json, LeadForge_Reboot\config\source-lanes.json, the latest run manifests under LeadForge_Reboot\data\runs, and git status.
Use config/source-lanes.json targetState as the selected state.
If the tree has uncommitted work, understand and commit durable in-scope changes before opening new work.
If no GitHub remote exists or gh is unauthenticated, keep local commits safe and retry publish only when a remote/auth path is available; do not stop lead production just because push is blocked.
Before each collector run, run LeadForge_Reboot\scripts\get-collector-guard-status.ps1 and only proceed when can_start_collector is true.
Do not merge raw leads. Stage collector outputs into a run folder, triage, enrich owners/decision makers from public sources, QA reviewed rows, reject suspicious or mismatched businesses, keep unresolved rows in pending with public_research_note, and merge only clean reviewed/final rows.
After each merge or rejection cycle, run contamination audit, owner backlog, pending report, ops snapshot, then commit.
If latest lanes produce no fresh leads or only fully rejected reviewed runs, rotate lanes and continue.
Preserve all existing files; never delete user/archive data. Only remove stale generated pending artifacts when the manifest has been updated and the replacement reviewed/rejected state exists.
"""
